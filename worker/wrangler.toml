# sleepy-jw-proxy — CORS 中转 Worker
# 部署属外部发布动作, 归用户批准; 本配置不绑定真实账户
# 部署: cd worker && npx wrangler deploy --name sleepy-jw-proxy
name = "sleepy-jw-proxy"
main = "sleepy-jw-proxy.ts"
compatibility_date = "2024-09-01"
account_id = ""  # 部署时填
