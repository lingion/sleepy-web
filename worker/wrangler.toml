# sleepy-jw-proxy — CORS 中转 Worker
# 已部署: https://sleepy-jw-proxy.lingion04.workers.dev (2026-09-12)
# 重部署: cd worker && CLOUDFLARE_API_TOKEN=<token> npx wrangler deploy
#   (token 位置: ~/.cloudflare-token; wrangler 复用 ~/proxy/node_modules)
# account_id 不写入此文件 — 部署时由 wrangler 从 token 推断
name = "sleepy-jw-proxy"
main = "sleepy-jw-proxy.ts"
compatibility_date = "2024-09-01"
